-- ═══════════════════════════════════════════════════════════════
-- ランキング評価：投稿の「お気に入りされた数」— posts.saved_count 列を追加
-- Supabase Dashboard → SQL Editor で実行してください。
--
-- お気に入り(保存)されるといいねより重く評価する(×30)ため、
-- 投稿ごとに保存された回数を集計する。お気に入り登録/解除で増減。
-- 未実行でも保存操作は壊れない(アプリ側で静かにスキップ)が、
-- 実行するまで saved_count は 0 のままでランキングに反映されない。
-- ═══════════════════════════════════════════════════════════════

ALTER TABLE posts ADD COLUMN IF NOT EXISTS saved_count INT DEFAULT 0;
