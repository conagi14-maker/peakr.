-- ═══════════════════════════════════════════════════════════════
-- オンボーディング（カテゴリー選択）完了状態をアカウント単位で保存
-- Supabase Dashboard → SQL Editor で実行してください。
--
-- 内容:
--  profiles に onboarded(boolean) を追加。
--  これにより「初回カテゴリー選択」は端末ごとではなく
--  アカウント単位で全端末通じて1回だけ表示されるようになります。
--  ※ 未実行でも動作します（その場合は従来どおり端末ごとに1回表示）。
-- ═══════════════════════════════════════════════════════════════

ALTER TABLE profiles ADD COLUMN IF NOT EXISTS onboarded BOOLEAN DEFAULT false;
