-- ═══════════════════════════════════════════════════════════════
-- ステージ：出演サムネイル画像 — activities.thumb_data 列を追加
-- Supabase Dashboard → SQL Editor で実行してください。
--
-- 出演登録時に任意でサムネ画像(base64データURL)を添付できるようにする。
-- 未実行でも登録自体は動く(アプリ側でサムネ抜きにフォールバック)が、
-- 実行するまでサムネは保存・表示されない。
-- ═══════════════════════════════════════════════════════════════

ALTER TABLE activities ADD COLUMN IF NOT EXISTS thumb_data TEXT;
